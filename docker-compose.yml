services:
  api:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      DATABASE_URL: sqlite:////app/data/app.db
    volumes:
      - ./:/app
      - app_data:/app/data

volumes:
  app_data:
